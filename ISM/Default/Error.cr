module ISM

    module Default

        module Error

            Title = "Internal error"
            Help = "#{ISM::Default::CommanLine::Name.upcase} raised that error because the ran script did not call properly a system command or the system command itself need to be fix."

        end

    end

end
