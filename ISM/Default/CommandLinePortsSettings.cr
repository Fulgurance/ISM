module ISM

    module Default

        module CommandLinePortsSettings

            TargetVersion = "master"
            PortsSettingsFilePath = "#{ISM::Default::Path::PortsDirectory}#{ISM::Default::Filename::PortsSettings}"

        end

    end

end
